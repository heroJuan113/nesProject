{{>navegacion/navegacion}}
<div class="container p-3">
    <div class="row">
        <div class="col-md-3">
            <div class="card">
                {{#each proveedores}}
                   <form action="http://127.0.0.1:4000/proveedores/editar/{{id}}/{{idUsuario}}/{{idSession}}" method="POST">
                    <div class="card-body"> 
                             {{> proveedores/formularioEdit}}
                            {{> botones/aceptar }}
                    </div>
                </form>
                {{/each}}
            </div>
        </div>
    </div>
</div>
